# Normalise line endings so the repo stays consistent across platforms.
* text=auto eol=lf

# Shell scripts must keep LF endings or they won't run.
*.sh text eol=lf

# Binary files must never be converted.
*.png binary
*.jpg binary
*.zip binary
